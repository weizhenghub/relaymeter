// v0.208：真实请求内容 —— 摘自用户中继数据库 relay.db（request 44658），
// 思考流（85824 字截取前 20000）+ 正文流（assistant，截取前 20000）。
// 生成方式：python 读 relay.db 的 messages 表 → JSON 序列化 → 本文件。
// 侧栏 __tourPanel 用它做真实流式演示（100 token/s，20000 字上限循环）。
window.TOUR_DEMO_TEXT = {"think": "Now I have a thorough understanding of panel_pool.py. Let me analyze the ball code and think about the design.\n\nKey observations:\n- `_create_ball_window()` creates a `webview.create_window` with `transparent=True`, `frameless=True`, `on_top=True`, `easy_drag=True`, `resizable=False`, `hidden=True`, sized `ball_size`.\n- `_on_ball_loaded()` does the WebView2 transparent hack via native form's TransparencyKey + set WS_EX_TOOLWINDOW.\n- `_show_ball` / `_hide_ball` move between pos and (-32000, -32000).\n- `_on_ball_moved(x,y)` records pos, clamps to screen, persists to settings, relayouts sidebar.\n- `_ball_push(js)` evaluates JS in ball window.\n- `ball_clicked()` — state machine.\n- `set_float_ball(enabled)` — toggle create/destroy.\n- `enforce_always_one()` — on/off, show/hide ball + clear S2.\n- `toggle_all_visible()` — hide/show ball with `_all_hidden`.\n- `assign()` / `_clear_rid()` — push ball visual state (flow/s2/idle).\n\nThe visual states: idle/flow/s2 via `ballSetState({mode:'...'})` JS.\n\nNow I understand the coordinate system. Note comments say `_ball_pos` is \"物理像素\". But `_on_ball_moved` clamps to `self._screen_size()`. And `_ball_push` evaluates JS. But the `set_float_ball_pos_from_panel` receives x/y = 侧栏左上角（物理像素）. Interesting - the existing code calls it \"physical pixels\" but actually pywebview .move() uses LOGICAL pixels (multiplies by _scale). So there's already some ambiguity in their comments. Let me be careful with the coordinate system in my design.\n\nActually let me re-read. The user says:\n- pywebview window `.move(x,y)` uses LOGICAL pixels and internally multiplies by `self._scale` (DPI, GetDpiForWindow/96) to physical.\n- So sidebar geometry and ball position are in LOGICAL pixels. The native layer must convert logical→physical via DPI scale.\n\nBut panel_pool comments say `_ball_pos` is physical pixels. This is a pre-existing confusion in their code. For my new native ball layer, I need to handle the conversion. The panel_pool methods `set_float_ball_pos_from_panel(x,y)` receive \"physical pixels\" per comment but the actual values passed around are what pywebview uses (logical). \n\nActually, given the ambiguous existing state, for the design I need to define clearly. Let me think about how the surrounding panel_pool uses `_ball_pos`:\n- `_default_ball_pos()`: returns `(mx + mw + 8, my + 8)` — mx,my,mw,mh come from `_dock_getter()` which returns LOGICAL pixels (per user: \"main_x, main_y, main_w, main_h in LOGICAL pixels\").\n- `_apply_geometry()` ball branch: `bx, by = self._ball_pos`; then width clamp `sw - bx - 4` where sw = screen_size logical. So `_ball_pos` is treated as LOGICAL in `_apply_geometry`.\n- `_on_ball_moved`: clamps `x` to `sw - 8` where sw = `_screen_size()` logical. So again logical.\n\nSo actually in the code, `_ball_pos` is LOGICAL pixels despite comments saying physical. The comments are misleading. Good — this is important. The ball position that panel_pool manages is in LOGICAL pixels.\n\nSo the native ball layer should ideally store/exchange LOGICAL pixels with panel_pool, and internally convert to physical for the actual native window. That keeps the call sites stable.\n\nNow, the user asked \"how to size/position with per-monitor DPI\". Given the existing pywebview system. Let me think about what to use for DPI. The main window's native has `_scale` property = GetDpiForWindow(hwnd)/96. But the ball lives on the desktop, potentially on a different monitor. For a layered window, we should get DPI per-monitor. Options:\n- GetDpiForWindow(hwnd) on the ball's own hwnd (after it's created on the monitor) — per-window DPI. Since it's a topmost popup it gets DPI its current monitor. But if it moves to another monitor without WM_DPICHANGED handling, DPI stays. \n- Use GetDpiForMonitor(monitor from window) — more complex.\n- Given the app already sets `SetProcessDPIAware()` (system DPI aware, via winforms.py:820), the app is System-DPI-Aware (not Per-Monitor). So GetDpiForWindow returns the system DPI. Actually `SetProcessDPIAware` = system aware. So DPI is uniform system-wide (the boot DPI). Hmm, but GetDpiForWindow with system-aware process returns system DPI. So per-monitor DPI varies only if... actually under System DPI awareness, Windows virtualizes coordinates and GetDpiForWindow returns the system DPI regardless of monitor.\n\nThis is an important nuance. Since the process is System-DPI-Aware (SetProcessDPIAware), all coordinates are effectively in system-DPI logical space, and GetDpiForWindow returns the system DPI. So the logical→physical conversion is `logical * (sysDpi/96)`. And it's uniform. But if they want true per-monitor, they'd need to call SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2) — but pywebview/WebView2 might not handle that well. So the design should: recommend matching the existing DPI awareness (System aware) to stay consistent, using the main form's `_scale` as the single source of truth, OR get per-monitor via GetDpiForMonitor on the monitor containing the ball, if the app is per-monitor aware.\n\nGiven the existing `_scale` on the BrowserForm is `GetDpiForWindow(hwnd)/96`, and the app is system-aware, the simplest robust approach is to reuse the SAME scale factor as the side panel uses (so ball and panel stay in the same coordinate space). That's critical: ball pos in logical, panel geometry in logical, screen size in logical — they all must use the same DPI factor. So use the main window's `_scale` (or equivalently GetDpiForWindow of the main hwnd) as the DPI factor for the ball. This avoids drift between ball and sidebar.\n\nActually wait — but the ball is topmost and may be on a second monitor. If the app is System-DPI-aware, then GetDpiForWindow on any window returns system DPI, so it's uniform. So reusing main window `_scale` is fine. Good.\n\nLet me now think about the layered window creation approach.\n\n### Layered window via UpdateLayeredWindow\n\nThe classic approach for per-pixel alpha:\n1. `CreateWindowExW(WS_EX_TOPMOST | WS_EX_LAYERED | WS_EX_TOOLWINDOW, ...)` with `WS_POPUP` style, zero-size window class (use a registered class or the built-in STATIC/\"#32770\" style). Actually for a layered window you typically use a window class. Common approach: register a class OR use the existing WinForms Form but that's complex with UpdateLayeredWindow. \n\nThere are two main approaches:\n- **Approach A: Raw HWND via CreateWindowExW + UpdateLayeredWindow with a DIB section.** Full control, truly transparent, no WinForms Form overhead. The HWND is created by raw CreateWindowExW and we pump UpdateLayeredWindow ourselves. This is the pure Win32 approach. Needs a window proc for WM_NCHITTEST / mouse handling.\n- **Approach B: WinForms Form with WS_EX_LAYERED ex-style + override OnPaint to use UpdateLayeredWindow (or GDI+ to a bitmap then SetLayeredWindowAttributes).** More pythonnet-friendly, can subclass Form. But UpdateLayeredWindow requires drawing to a 32bpp premultiplied alpha DIB, which is more work than a normal OnPaint. Alternatively use `SetLayeredWindowAttributes(opacity, colorKey, LWA_ALPHA)` for per-window alpha (uniform opacity) — but that gives uniform alpha, NOT per-pixel. For a circle, uniform alpha won't work (you need alpha=0 outside circle). \n\nActually, the simplest GDI+ per-pixel-alpha way with a WinForms Form is:\n- Create the Form (frameless, WS_EX_LAYERED via `exstyle |= WS_EX_LAYERED`).\n- In OnPaint / or via a Bitmap of 32bppArgb drawn with GDI+, then call UpdateLayeredWindow pointing at a DIB section created from that Bitmap's HBITMAP. \n\nThe cleanest is really Approach A with raw HWND and a memory DC + DIBSection. But doing that via pythonnet/ctypes is more code. However, given pythonnet is available and the Form approach already has quirks (the ShowInTaskbar deadlock they hit), a raw HWND might actually be MORE robust because we fully control the message loop and don't inherit WinForms' threading model.\n\nLet me weigh:\n- Raw HWND: needs RegisterClass/WndProc (must be a native callback — can't be a Python callable directly via ctypes WNDPROC because that has GC issues; but we can use the default window proc and handle WM_NCHITTEST via... actually we need a WNDPROC for mouse messages. pythonnet can pass delegates to SetWindowLongPtr but keeping a reference is tricky. ctypes WNDPROC with a Python callback is doable but must keep a strong ref and deal with callback lifetime). This is the classic hard part.\n\nActually, a common trick: you can avoid a real WNDPROC by doing mouse tracking via polling (`GetCursorPos` + `GetAsyncKeyState` in a timer) rather than handling WM_LBUTTONDOWN. Many layered-window implementations use a low-level approach. But for smooth drag you want WM_NCHITTEST → HTCAPTION so the system handles drag. That needs a WNDPROC.\n\nAlternatively: use a WinForms Form for the mouse handling (it has Move/Resize/MouseDown/MouseUp events, and the WinForms message loop already runs), but render via UpdateLayeredWindow. Let's think:\n\n**Approach B refined: WinForms Form subclass `BallForm`**\n- Subclass `System.Windows.Forms.Form`.\n- Set `FormBorderStyle = None`, `ShowInTaskbar = False`, `TopMost = True`, `StartPosition = Manual`.\n- Add `WS_EX_LAYERED` to ex-style, or set `TransparencyKey` (but that's what failed). Actually with a Form we can do BOTH: set BackColor/TransparencyKey for the \"dirty\" combos... no. \n\nTo get per-pixel alpha with a Form, the technique is:\n- Create a `Bitmap` (Format32bppArgb) of the ball size.\n- Draw the circle with GDI+ onto the Bitmap (using a Graphics created from the Bitmap). Alpha outside circle = 0, inside = varied (with gradient).\n- Get the HBITMAP, create a DIB section memory DC, call UpdateLayeredWindow(hwnd, hdcScreen, pptDst, psize, hdcSrc, pptSrc, 0, ref blend, 0).\n- Handle the window's own painting by NOT letting the form do normal GDI painting (don't call base OnPaint; set the window to not repaint itself). Actually with a layered window using UpdateLayeredWindow, the window content is entirely defined by the last UpdateLayeredWindow call; WM_PAINT painting won't show. So we just need to call UpdateLayeredWindow whenever the visual changes.\n\nThe Form gives us: existing message loop (it's on the UI thread which is the same STA message loop as the main window), MouseDown/MouseUp/MouseMove events (via WS_POPUP it will get mouse messages if it's not covered... but WM_NCHITTEST for drag). \n\nHmm, but the user specifically said \"Whether a System.Windows.Forms.Timer for animation, driven on the UI thread, is appropriate\". So they're open to Form + Timer.\n\nLet me think about which is cleanest in pythonnet given the project's constraints:\n\nThe main window is created on a dedicated STA thread (see winforms.py:825-827: `thread = Thread(ThreadStart(create)); thread.SetApartmentState(ApartmentState.STA); thread.Start()`, then `app.Run()`). Actually for the 'master' window, the STA thread runs `create()` which calls `app.Run()`. All other windows are created via `i.Invoke(Func[Type](create))` on that same UI thread (line 838-839). So ALL WinForms objects live on ONE STA UI thread. That's the pywebview GUI thread.\n\nSo the ball Form MUST be created and manipulated on THAT STA thread. The PanelPool has `app.window.native` = the master BrowserForm (a WinForms Control). We can use `native.BeginInvoke(MethodInvoker(...))` to marshal UI work onto that STA thread. This is exactly what the existing `_on_ball_loaded` does: `native.BeginInvoke(MethodInvoker(_apply))`.\n\nSo the design: BallLayer holds the main BrowserForm reference (`main_native`), exposes a `_dispatch(fn)` that does `main_native.BeginInvoke(MethodInvoker(fn))` if `InvokeRequired`, else calls directly. All Form creation, all UpdateLayeredWindow, all mouse handlers run on the UI thread via this dispatch.\n\nKey threading bug to avoid (the ShowInTaskbar deadlock): A cross-thread deadlock can happen if the UI thread is blocked waiting on a lock while a Python thread tries to BeginInvoke. The classic path: Python thread holds `pool._lock`, then calls BeginInvoke and WaitOne (blocking) → UI thread tries to acquire `pool._lock` (or something) → deadlock. The fix per the project's pattern: never block on Invoke, use BeginInvoke (fire-and-forget) which doesn't block the calling thread. And never hold `_lock` while calling into the UI. In panel_pool, calls like `_ball_push` go through `_enqueue_op` (op worker) then call evaluate_js which internally does BeginInvoke. So the pattern is: don't hold `_lock` when dispatching, and use fire-and-forget dispatch.\n\nFor BallLayer, I'll design so that:\n- Public methods called from Pythch threads (SSE thread, op worker, execute thread) do state updates under the pool's lock and then schedule a fire-and-forget `_dispatch` to the UI thread for the actual native work.\n- All native window calls are on the UI thread.\n- No method blocks waiting for the UI thread to return.\n\n### Now the rendering: circle with GDI+\n\nPer-pixel alpha DIB:\n- Size = ball_size * scale (physical). \n- Create a 32-bit top-down DIB section with BI_RGB.\n- Get the DIB bits and fill with premultiplied BGRA (blue, green, red, alpha). Note UpdateLayeredWindow expects premultiplied alpha (BLENDFUNCTION with AC_SRC_ALPHA means source colors are premultiplied by alpha).\n- For the circle gradient, compute per-pixel. But GDI+ can't easily write premultiplied alpha into a DIBSection directly with nice anti-aliasing unless we use GraphicsPath/PathGradientBrush and render to a 32bppArgb Bitmap, then extract the bits (with premultiply). GDI+ Bitmap is straight (non-premultiplied) alpha by default; we must premultiply before UpdateLayeredWindow.\n\nDesign choice: Render with GDI+ into a System.Drawing.Bitmap (Format32bppArgb). Use GraphicsPath.AddEllipse + PathGradientBrush (radial gradient) to draw the ball with anti-aliasing (SmoothingMode=AntiAlias). This is clean and pythonnet-friendly since System.Drawing is already referenced. Then lock the bitmap's bits, iterate per-pixel, premultiply BGRA, write into the DIBSection, then UpdateLayeredWindow.\n\nPremultiplied alpha conversion: For each pixel with (a, r, g, b) straight alpha:\n- premult: R = r*a/255, G = g*a/255, B = b*a/255, A = a.\n- Store as BGRA in the DIB: byte0=B, byte1=G, byte2=R, byte3=A.\n\nAlternatively, GDI+ can render directly into a 32bppPArgb bitmap (premultiplied) via `Bitmap` with `PixelFormat.Format32bppPArgb`. Actually `System.Drawing` has `ImageLockMode`. If we create a Bitmap with Format32bppPArgb, GDI+ draws with premultiplied alpha automatically, and then the raw bytes are already premultiplied. That avoids manual premultiply. But pythonnet memory sharing is subtle. I'll present both options — recommend rendering to Format32bppPArgb so bytes are premultiplied, then memcpy into the DIB.\n\nActually the cleanest: create the DIBSection ourselves via CreateDIBSection, then wrap the buffer from a type that GDI+ draws into using a Bitmap created from the borrowed HBITMAP? Hmm. Let me not over-engineer. Recommending: \n1. `Bitmap bmp(ball_phys, ball_phys, Format32bppPArgb)`. Note PArgb = premultiplied.\n2. `Graphics g(bmp)`; set SmoothingMode=AntiAlias.\n3. Build GraphicsPath ellipse, PathGradientBrush (center→edge radial gradient). Fill.\n4. For s2: draw red dot badge + pulse ring.\n5. Lock bits (`LockBits`), copy the row bytes into the DIBSection bytes (they're already premultiplied BGRA since PArgb).\n6. `UpdateLayeredWindow(hwnd, screenDC, dst, size, memDC, src, 0, ref blend, ULW_ALPHA)`.\n\nFunction signature:\n```\nBOOL UpdateLayeredWindow(\n  HWND hwnd,             // layered window\n  HDC hdcDst,            // screen DC\n  POINT *pptDst,         // screen position of window\n  SIZE *psize,           // window size\n  HDC hdcSrc,            // DC of source bitmap\n  POINT *pptSrc,         // top-left of source\n  COLORREF crKey,\n  BLENDFUNCTION *pblend, // AC_SRC_OVER, AC_SRC_ALPHA\n  DWORD dwFlags          // ULW_ALPHA\n);\n```\n\nNeed `GetWindowDC(hwnd)` or `GetDC(NULL)` for hdcDst. Must lock the update with `SetWindowLongPtr`? Actually the standard pattern: `HDC hdcScreen = GetDC(NULL)`. GetWindowDC also ok. Some use `GetDCEx`.\n\nDPI: In the design I'll say the ball DIB dimensions and the pptDst position use PHYSICAL pixels computed as `logical * scale`. Since the app is System-DPI-aware, `scale` = `GetDpiForWindow(main_hwnd)/96`. And we update position via UpdateLayeredWindow's pptDst each redraw (no need for SetWindowPos at all). Actually UpdateLayeredWindow both positions and paints the window in one call. But we also need window-level position semantics for setting the window's coordinates. Actually for a layered window moved only via UpdateLayeredWindow, tracking clicks & drags needs WM_NCHITTEST → HTCAPTION or manual tracking, and the OS's notion of window position (GetWindowRect) reflects pptDst after UpdateLayeredWindow. So dragging: on WM_LBUTTONDOWN, use WM_NCHITTEST = HTCAPTION trick, or set the window into a \"moving\" state and update via WM_MOUSEMOVE.\n\nActually simplest robust drag for layered windows: handle WM_NCHITTEST to return HTCAPTION when cursor is over the ball. Then Windows drags the window automatically. But with UpdateLayeredWindow, the drag also fires WM_MOVING/WM_WINDOWPOSCHANGED which we can catch to repaint at the new position. Hmm, but if the window is entirely rendered by UpdateLayeredWindow and we never call it again during a system drag, the ball won't move visually because the layered window content is fixed. Actually when Windows drags a window (via HTCAPTION), it moves the window; for a layered window, UpdateLayeredWindow content stays but the window is repositioned — but the DIB was painted at the old pptDst. Moving the window via the system changes GetWindowRect but the rendered content stays at old location within the window... Actually the layered window's content is painted in window (client) coordinates, and the window origin moves, so the whole ball moves with it. WM_PAINT won't be needed; the layered content just translates with the window. Yes — a layered window's surface moves with the window. So HTCAPTION drag works and the ball translates. On WM_MOUSEMOVE or WM_EXITSIZEMOVE... we get WM_MOVING. We can handle drag-end via WM_EXITSIZEMOVE or the Form's Move event.\n\nBut there's a subtlety: with HTCAPTION drag, the mouse messages are consumed by the system move loop and we don't get simple MouseUp. The Form's `Move` event fires continuously during drag, and `MouseUp`/deactivate. To detect drag-end we can hook the Form's `Move`/`LocationChanged` and compare with a saved pos; but for \"click vs drag\" we want to detect whether movement exceeded threshold. With HTCAPTION the initial click-down is WM_NCHITTEST then the system starts a modal move loop from the WM_NCLBUTTONDOWN. We don't get a normal MouseDown. So detecting click vs drag with HTCAPTION is harder.\n\nAlternative: manual drag tracking (mouse down/move/up) — more control, gives us click vs drag cleanly.\n- On WM_LBUTTONDOWN (over the ball): record mouse-down screen pos and window pos. Set a dragging flag.\n- On WM_MOUSEMOVE: if dragging, `SetWindowPos` (or UpdateLayeredWindow with new pptDst) to follow `cur_screen - down_delta`. Repaint as it moves.\n- On WM_LBUTTONUP: if total movement < threshold → it's a click → call `ball_clicked()`. Else it's a drag → persist new pos.\n\nManual tracking requires a WNDPROC on the layered window. Handling WM_NCHITTEST not needed (we don't return HTCAPTION since we track manually). But we still need the window to receive WM_LBUTTONDOWN etc. Since the window is WS_POPUP and topmost and covers just the ball area, mouse over the ball region hits it. But clicks outside the circle (alpha=0 region) — a layered window still gets hit-tested by its window rect, not by alpha. So the whole square window would capture clicks, including the transparent corners. To avoid that, we could set a region (SetWindowRgn) to a circle so hit-testing respects the circle, OR handle WM_NCHITTEST to return HTTRANSPARENT (for the parts outside the ball). Best: us", "body": "I have what I need. Here's the design.\n\n---\n\n# Design: Native per-pixel-alpha layered ball — `BallLayer`\n\nScope note on coordinates: I verified in `panel_pool.py` that `_ball_pos` is actually used in **LOGICAL** pixels throughout (`_apply_geometry` clamps it against logical `_screen_size()`; `_default_ball_pos` uses the logical `_dock_getter`). The comments calling it \"物理像素\" are misleading. The swap must therefore: keep ALL PanelPool↔ball exchanges in logical pixels, and convert to physical only inside `BallLayer` at the `UpdateLayeredWindow` boundary. This preserves the sidebar-anchor invariant and every existing call site.\n\nThe ball window is currently a third WebView2 created at `panel_pool.py:336` (`transparent=True`, `frameless=True`, `on_top=True`, `easy_drag=True`) with its transparency hack in `_on_ball_loaded` (`panel_pool.py:398-443`). All of that is replaced.\n\n---\n\n## 1. `BallLayer` class — new module `src/relay/ball_layer.py`\n\nRecommended: new self-contained module, not inline in `panel_pool.py`. Rationale: it carries `import clr` / `import System.Drawing` and Win32 ctypes that must degrade silently on non-Windows / non-winforms backends; keeping it isolated lets `panel_pool.py` guard the import and no-op cleanly.\n\n```\nclass BallLayer:\n    def __init__(self, *, main_native, ball_size, scale_getter, theme_bg,\n                 on_click, on_move_end):\n        # main_native  = app.window.native  (master BrowserForm, a WinForms Control)\n        # scale_getter = callable -> float  (logical->physical; use main_native._scale)\n        # on_click     = lambda with NO args            -> panel_pool.ball_clicked\n        # on_move_end  = lambda (logical_x, logical_y)  -> panel_pool._persist_ball_pos\n```\n\nResponsibilities and fields:\n- `_form` — a `BallLayerForm( WinForms.Form )` created lazily on the UI thread; `None` if unavailable/fallback.\n- `_mode` — `\"idle\" | \"flow\" | \"s2\"` (thread-safe, guarded by `self._lock`).\n- `_pos` — logical `(x, y)`; **the single source of truth for position** (never trust `Form.Location`).\n- `_frame` — integer counter advanced by the animation Timer.\n- `_timer` — a `WinForms.Timer`; started on entering `flow`/`s2`, stopped on `idle`.\n- `_dragging`, `_down_screen`, `_down_loc` — manual-drag state.\n- A cached 32bpp DIBSection + `hBitmap` + memory DC, recreated only when the physical size changes.\n\nThread-safe **public** API (callable from any thread; each does state update then fire-and-forget dispatch to the UI thread):\n- `start(logical_x, logical_y)` — dispatch: create form on UI thread, pre-render + position via `UpdateLayeredWindow` while hidden, then `Show()`.\n- `destroy()` — dispatch: stop timer, close form, release DIB. `/` no-op if `_form is None`.\n- `show()` / `hide()` — dispatch `ShowWindow(SW_SHOW/SW_HIDE)` (layered surface persists across hide; position preserved, so no `-32000` hack needed).\n- `move_to(logical_x, logical_y)` — set `_pos`, dispatch position-only `UpdateLayeredWindow` (no redraw). Called from `set_float_ball_pos_from_panel`.\n- `set_state(mode)` — set `_mode`, dispatch redraw, start/stop the timer. Called from `assign`/`_clear_rid`/`ball_clicked` (replaces `_ball_push`).\n\nUI-thread-only internals:\n- `_present(new_logical, *, redraw)` — the one function that calls `UpdateLayeredWindow`. If `redraw`, rebuild the premultiplied DIB from `_mode`+`_frame`, then blit; otherwise reposition only (reuse cached source DC). Converts `logical*scale` → `ptDst` and `ball_size*scale` → `psize` **every call**, defensively.\n- `_redraw()` — GDI+ draw into a `Format32bppPArgb` bitmap → `LockBits` → copy rows into the DIBSection buffer → `UpdateLayeredWindow`.\n- `_on_mouse_down/_on_mouse_move/_on_mouse_up` — drag + click logic (see §3).\n- `_tick()` — timer handler: `_frame += 1; _present(_pos, redraw=True)`.\n\nConnection to PanelPool (all at the native boundary): `main_native = self._api._app.window.native`; `scale_getter = lambda: float(main_native._scale)` (reuse the SAME scale as the panel so ball and sidebar stay in one coordinate space); `on_click = self.ball_clicked`; `on_move_end = self._persist_ball_pos`.\n\n---\n\n## 2. Exact Win32 calls\n\n**Window styles** (set on the Form via `SetWindowLongPtrW(GWL_EXSTYLE, ex | WS_EX_LAYERED)` before first `Show()`; `GWL_EXSTYLE=-20`):\n- `WS_EX_LAYERED  = 0x00080000` — enables per-pixel alpha.\n- `WS_EX_TOPMOST  = 0x00000008` — stays on top.\n- `WS_EX_TOOLWINDOW = 0x00000080` — removes from taskbar/alt-tab (`Form.ShowInTaskbar=False` also helps).\n- `WS_EX_NOACTIVATE = 0x08000000` — prevents focus stealing on click.\n- Also clear `WS_EX_APPWINDOW = 0x00040000`.\n- `Form` styles: `FormBorderStyle = None`, `StartPosition = Manual`, `ShowInTaskbar = False`, `TopMost = True`, `ResizeRedraw = False`, `MaximizeBox = False`, `SetStyle(ControlStyles.Opaque | UserPaint | AllPaintingInWmPaint, True)`.\n\nDo NOT use `SetLayeredWindowAttributes` (uniform opacity → not per-pixel) and do NOT use `LWA_COLORKEY` (→ black corners) or a WebView2 surface. Per-pixel alpha requires `UpdateLayeredWindow` + 32bpp premultiplied DIB.\n\n**Corner click-through** (optional but recommended): `SetWindowRgn(hwnd, CreateEllipticRgn(0,0,w_phys,h_phys), True)` then `DeleteObject`. This makes the OS hit-test only the circle, so alpha=0 corners are pass-through. If region+layered misbehaves, fall back to rejecting clicks outside the radius inside `MouseDown` (works, just corners not pass-through).\n\n**DIBSection** (BI_RGB, 32bpp, top-down):\n```\nBITMAPINFOHEADER:\n  biSize = 40, biWidth = w_phys, biHeight = -h_phys (top-down), biPlanes = 1,\n  biBitCount = 32, biCompression = BI_RGB (0), rest 0\nCreateDIBSection(hdcScreen, &bmi, DIB_RGB_COLORS(0), &pBits, NULL, 0) -> hBitmap\nhdcMem = CreateCompatibleDC(hdcScreen); SelectObject(hdcMem, hBitmap)\n```\n`pBits` is the raw 32bpp BGRA (blue,green,red,alpha) buffer. Keep a live `ctypes` ref to it.\n\n**BLENDFUNCTION** (AC_SRC_OVER + AC_SRC_ALPHA → source is premultiplied):\n```\nstruct BLENDFUNCTION { BYTE BlendOp=0; BYTE BlendFlags=0; BYTE SourceConstantAlpha=255; BYTE AlphaFormat=1; }\n```\n\n**UpdateLayeredWindow** — the one call that both positions and paints (this is why all geometry goes through it, avoiding layered-window ghosting from `SetWindowPos`):\n```\nUpdateLayeredWindow(hwnd, hdcScreen, &POINT(x_phys,y_phys), &SIZE(w_phys,h_phys),\n                    hdcMem, &POINT(0,0), 0, &blend, ULW_ALPHA(0x00000002))\n```\n`hwnd` = `form.Handle.ToInt32()`; `hdcScreen = GetDC(NULL)`.\n\n**GDI+ rendering** into the premultiplied DIB (recommended: draw into a `System.Drawing.Bitmap(Format32bppPArgb)` so GDI+ premultiplies automatically, then `LockBits` and memcpy into `pBits`):\n- `g.SmoothingMode = AntiAlias`, `g.Clear(Color.Transparent)`.\n- Base ball: `GraphicsPath.AddEllipse(0,0,w,h)` + `PathGradientBrush` (radial: light center → darker edge) or a `LinearGradientBrush` (top-left light → bottom-right dark). Colors derive from `theme_bg`.\n- `idle`: static fill only.\n- `flow`: outer soft glow ring whose alpha/width oscillates with `_frame % k` (\"active\" feel).\n- `s2`: distinct pulse ring + a **red dot badge** (e.g. 12px) at top-right.\n- `LockBits(rect, ReadOnly, Format32bppPArgb)` → `BitmapData`; copy each row by `Stride` into `pBits` (DIB is top-down so row index maps directly).\n\n**DPI sizing/positioning**: `scale = scale_getter()` recomputed on every `_present`. `w_phys = h_phys = int(ball_size * scale)`; `ptDst = (int(x_logical*scale), int(y_logical*scale))`. Because the app runs `SetProcessDPIAware()` (system-aware, winforms.py:820) and the panel already uses `native._scale = GetDpiForWindow(hwnd)/96`, reuse that single scale — uniform system DPI, so ball and sidebar can never drift. Flag: if you later make the app per-monitor-aware, `_present` must also handle `WM_DPICHANGED` (rescale + reshape) — noted as a limitation.\n\n---\n\n## 3. Threading & marshaling model (deadlock-safe)\n\nAll WinForms objects live on the single pywebview STA UI thread (`winforms.py:825-839`: master form created on an STA `Thread`, then `app.Run()` pumps every top-level window on that thread). `BallLayer` rides that same thread.\n\n- **Dispatcher:** hold `self._main_native` (a `Control`). \n  ```\n  def _dispatch(self, fn):\n      mi = System.Windows.Forms.MethodInvoker(fn)\n      if self._main_native.InvokeRequired:\n          self._main_native.BeginInvoke(mi)   # ASYNC — never Invoke()/WaitOne()\n      else:\n          fn()\n  ```\n  `BeginInvoke` (not `Invoke`) is mandatory to avoid the cross-thread deadlock class you already hit with `ShowInTaskbar`. Never block a Python thread waiting for the UI thread.\n\n- **Form creation** → `_dispatch(_create)`. **`UpdateLayeredWindow` / GDI+ / mouse handlers / `WinForms.Timer.Tick`** → all run on the UI thread (timer Tick is inherently on the UI thread).\n\n- **Lock discipline:** never call `_dispatch` while holding `pool._lock`. PanelPool methods update state under `_lock`, release, then dispatch. `BallLayer`'s own `_lock` guards only `_mode`/`_pos`/`_visible` (cheap, never held across a dispatch).\n\n- **Mouse drag + click** (deliberately NOT `HTCAPTION` — the system drag loop would swallow `MouseUp` and make click-vs-drag detection hard):\n  - `MouseDown` (left): `_down_screen = Cursor.Position` (physical), `_down_loc = _pos_phys`, `_dragging=True`, `this.Capture=True`.\n  - `MouseMove`: if `_dragging`, `new_phys = _down_loc + (Cursor.Position - _down_screen)`, `_present(new_phys/scale, redraw=False)` (reposition only).\n  - `MouseUp`: if `_dragging`; if `|Cursor.Position - _down_screen| < ~4 phys px` → **click** → `on_click()` (→ `ball_clicked`); else → **drag ended** → `on_move_end(logical_x, logical_y)` (→ persist + relayout).\n  - Because all geometry is UpdateLayeredWindow-centric and `_pos` is the source of truth, this avoids `SetWindowPos`/`Form.Location` ghosting and any WinForms location caching.\n\n- **Callbacks offloaded:** `on_click` / `on_move_end` are invoked on a fresh Python thread (`threading.Thread(target=...).start()`), NOT inline on the UI thread. This is the key deadlock breaker — the UI thread never needs to acquire `pool._lock`, so a Python thread holding `_lock` can never deadlock against the UI thread.\n\n- **Timer choice:** use a `System.Windows.Forms.Timer` (Tick on the UI thread, Interval ≈ 33ms ≈ 30fps; `timer.Tick += self._tick; timer.Start()`). A Python `threading.Timer` + `BeginInvoke` would queue 20-30 cross-thread messages/sec and risk ordering/backlog — that's why the UI-thread timer is safer. Start only in `flow`/`s2`, stop in `idle` (static ball, no CPU). Timer Ticks never overlap (serialized on the UI thread), so no re-entrancy. This mirrors the existing master-form timer in `winforms.py:803-806`.\n\n---\n\n## 4. Risks and fallbacks\n\n1. **GDI+/pythonnet fails, or non-Windows / non-winforms backend.** Wrap the `clr`/`System.Drawing` imports and every native call in `try/except`. On failure set `_form=None`, `available=False`, log once, and let `_create_ball` set `self._ball=None`. Ball silently degrades to OFF (safe no-op) — **do not** fall back to a solid/form square or `TransparencyKey`, since that is the previously-failed path. The strongest degradation is \"no ball\", never \"wrong-looking ball\".\n2. **`UpdateLayeredWindow` move ghosting.** Mitigated by always positioning through `UpdateLayeredWindow` (never `SetWindowPos`). Test the actual system; if a ghost persists, reprocess the circle region on each frame.\n3. **DPI mismatch.** Reuse `main_native._scale` as the single factor so ball and sidebar can't diverge. Process is System-DPI-aware → factor is uniform; still recompute every `_present` defensively. Limitation flagged: no `WM_DPICHANGED` handling today.\n4. **Threading deadlock.** Enforced by three rules: `BeginInvoke` only, never hold `pool._lock` while dispatching, and offload `on_click`/`on_move_end` off the UI thread. This is the direct antidote for the `ShowInTaskbar` deadlock.\n5. **Focus stealing / taskbar.** `WS_EX_NOACTIVATE` + `ShowInTaskbar=False` + `WS_EX_TOOLWINDOW` set before first `Show`. Test that the main window keeps focus.\n6. **Animation cadence.** UI-thread `WinForms.Timer` at ~30fps; stop when idle. If UI ever stutters, drop to ~20fps rather than a cross-thread timer.\n7. **Corner click-through.** `SetWindowRgn`(circle); if region+layered conflicts, reject outside-radius clicks in `MouseDown` (corners become non-pass-through but visually still transparent).\n8. **Black/white corners.** Guaranteed clear by true per-pixel alpha (`Format32bppPArgb` + `AC_SRC_ALPHA`). Re-verify the DIB is 32bpp and premultiplied — this is the only correct path; never `SetLayeredWindowAttributes`/`LWA_COLORKEY`.\n9. **`_ball_size` unit.** Documented as \"物理像素\" but consumed as logical by the current `create_window`; keep it **logical** in `BallLayer` and convert via `scale` (consistent with panel geometry).\n\n---\n\n## 5. Minimal changes to `src/relay/panel_pool.py`\n\nSwap the WebView2 ball for `self._ball: BallLayer | None`. Preserve every call site; only internals change.\n\n- Constructor: `self._ball_window` → `self._ball = None`. Keep `_ball_pos` (logical) and `_float_ball_enabled`. Keep `float_ball_url` param for signature compatibility but ignore it (no more WebView2).\n- **`_create_ball_window()` → `_create_ball()`**: `try: from relay.ball_layer import BallLayer`; build `BallLayer(main_native=self._api._app.window.native, ball_size=self._ball_size, scale_getter=lambda: float(main_native._scale), theme_bg=self._theme_bg, on_click=self.ball_clicked, on_move_end=self._persist_ball_pos)`; call `self._ball.start(*default_pos)`. On any exception or `ImportError` → `self._ball=None` (silent). Delete the `webview.create_window(...)` and the `_on_ball_loaded` transparency hack.\n- **`_show_ball()`** → `if self._ball: self._ball.show()`.\n- **`_hide_ball()`** → `if self._ball: self._ball.hide()` (no `-32000` move needed).\n- **`_on_ball_moved(...)`** → rename to **`_persist_ball_pos(self, x_logical, y_logical)`** receiving logical coords directly (BallLayer converts internally). Body = existing clamp (`_screen_size()`) + save settings + `_relayout()`. Update `on_move_end` binding to this.\n- **`set_float_ball_pos_from_panel(x, y)`** → set `_ball_pos` + `_ball.move_to(x, y)` (logical). Fix the misleading \"物理像素\" comment to LOGICAL here and in `_on_ball_moved`.\n- **`_ball_push(js)`** → replace with **`_set_ball_state(mode)`** = `if self._ball: self._ball.set_state(mode)`. Update call sites: `ball_clicked` (`panel_pool.py:514,522`), `assign` (`:608,612`), `_clear_rid` (`:709`) — replace `_ball_push(\"ballSetState({mode:'X'})\")` with `_set_ball_state(\"X\")`.\n- **`ball_clicked`**, **`set_float_ball`**, **`enforce_always_one`**, **`toggle_all_visible`** → same logic, but use `_create_ball`, `_show_ball`, `_hide_ball`, `_ball.destroy()` instead of window create/destroy.\n- **`stop()`** → `self._ball.destroy()` instead of `_ball_window.destroy()`.\n- `_apply_geometry` ball branch, `_default_ball_pos`, `_screen_size`, `ball_mode` → unchanged (already logical).\n\nNo changes needed in `gui.py`, `main.py`, or the HTML; the `ballSetState`/WebView2 pipeline is gone, replaced by `set_state`.\n\n---\n\nKey files: `C:\\Users\\weizheng\\PycharmProjects\\Usage_stats\\src\\relay\\panel_pool.py` (rewrite ball internals), new `C:\\Users\\weizheng\\PycharmProjects\\Usage_stats\\src\\relay\\ball_layer.py` (the `BallLayer` + `BallLayerForm` classes)."};

window.TOUR_DEMO_TEXT.tool = {"name": "Read", "input": {"file_path": "C:\\Users\\weizheng\\PycharmProjects\\Usage_stats\\src\\relay\\gui.py", "offset": 60, "limit": 15}};
